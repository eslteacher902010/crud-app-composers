<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Composer</title>
    <link rel="stylesheet" href="/style/style.css" />
  </head>

  <body>
    <div class="form-page">
      <h1>Edit Composer</h1>

      <form action="/composers/<%= composer.apiId %>?_method=PUT" method="POST">
        <div class="form-group">
          <label for="sex">Sex:</label>
          <input type="text" id="sex" name="sex" value="<%= composer.sex %>" />
        </div>

        <div class="form-group">
          <label for="nationality">Nationality</label>
          <input type="text" id="nationality" name="nationality" value="<%= composer.nationality %>" />
        </div>

        <div class="form-group">
          <label for="biography">Biography</label>
          <textarea id="biography" name="biography"><%= composer.biography %></textarea>
        </div>

        <div class="form-group">
          <label for="notes">Notes (only visible to you)</label>
          <textarea id="notes" name="notes"><%= userComposer?.notes || "" %></textarea>
        </div>

        <button type="submit">Save Changes</button>
      </form>

      <a class="back-link" href="/composers">Back to Composers</a>
    </div>
  </body>
</html>
