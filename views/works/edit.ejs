<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Work</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>

  <body>
    <section class="treble">
      <div class="edit-page">
        <h1>Edit Work</h1>

        <p><strong>Note:</strong> The purpose of this page is to just add more information. 
        Not to change the database’s existing information (except for local works).</p>

        <!-- ✅ Update form wraps everything -->
        <form action="/works/<%= work.apiId %>?_method=PUT" method="POST">

          <!-- ✅ Editable title if local, read-only otherwise -->
          <% if (work.source && work.source.toLowerCase().trim() === "local") { %>
            <div class="form-group">
              <label for="title">Title:</label>
              <input 
                type="text" 
                id="title" 
                name="title" 
                value="<%= work.title %>" 
                required
              />
            </div>
          <% } else { %>
            <h3><%= work.title || work.subtitle %></h3>
          <% } %>

          <% if (work.composer) { %>
            <div class="form-group">
              <label for="composer">Composer:</label>
              <p>
                <a href="/composers/<%= work.composer.apiId %>">
                  <%= work.composer.completeName %>
                </a>
                |
                <a href="https://openopus.org/composer/detail/<%= work.composer.apiId %>" target="_blank">
                  OpenOpus Profile
                </a>
              </p>
              <input type="hidden" id="composer" name="composerInput" value="<%= work.composer._id %>" />
            </div>
          <% } else { %>
            <div class="form-group">
              <label for="composer">Composer (apiId or name):</label>
              <input 
                type="text" 
                id="composer" 
                name="composerInput" 
                placeholder="Enter OpenOpus composer ID or name" 
              />
              <small>
                Tip: You can look up a composer on 
                <a href="https://openopus.org/composer/list/popular" target="_blank">OpenOpus</a>
              </small>
            </div>
          <% } %>

          <div class="form-group">
            <label for="genre">Genre:</label>
            <input 
              type="text" 
              id="genre" 
              name="genreInput" 
              value="<%= work.genre || '' %>" 
              placeholder="Enter genre (e.g. Symphony, Chamber, Opera...)" 
            />
          </div>

          <div class="form-group">
            <label for="yearComposed">Year Composed:</label>
            <input type="text" id="yearComposed" name="yearComposed" value="<%= work.yearComposed %>" />
          </div>

          <div class="form-group">
            <label for="catalogueSystem">Catalogue System (e.g. "K" in Mozart's K.545):</label>
            <input type="text" id="catalogueSystem" name="catalogueSystem" value="<%= work.catalogueSystem %>" />
          </div>

          <div class="form-group">
            <label for="catalogueNumber">Catalogue Number (e.g. the number after the K in Mozart's K. 545):</label>
            <input type="text" id="catalogueNumber" name="catalogueNumber" value="<%= work.catalogueNumber %>" />
          </div>

          <div class="form-group">
            <label for="youTube">YouTube:</label>
            <input type="text" id="youTube" name="youTube" value="<%= work.youTube %>" />
          </div>
        
          <button type="submit">Save Changes</button>
        </form>

        <a class="back-button" href="/">Back to Home</a>
      </div>
    </section>
  </body>
</html>
